// Use the `shift()` method to remove the first element from an array.

let arr = [10, 20, 30, 40, 50];
function removeFirstElement() {
  let n = arr.shift();
  console.log(`the first element removed : ${n}`);
}
removeFirstElement();
removeFirstElement();
removeFirstElement();
