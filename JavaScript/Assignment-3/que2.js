// Create a JavaScript program that removes the last element from an array using the `pop()` method.
let arr = [10, 20, 30, 40, 50];
function popElement() {
  let n = arr.pop();
  console.log(`poped element is ${n}`);
}
popElement();
popElement();
popElement();
